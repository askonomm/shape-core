<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="initial-scale=1.0, width=device-width" />
    <title>Create account - Shape</title>
    <link rel="stylesheet" href="/style.css" />
  </head>
  <body
    class="bg-slate-100 w-full min-h-screen flex flex-col items-center justify-center"
  >
    <h2 class="text-3xl font-semibold mb-8">Create account</h2>

    <form
      method="post"
      class="p-8 bg-white sm:border sm:border-slate-400 sm:w-[400px] w-full sm:rounded-md shadow"
    >
      <input type="hidden" name="csrf_token" value="{$csrf_token}" />

      {if $errors}
        <ul class="block mb-6 text-red-500">
          <h3 class="text-xs uppercase font-semibold mb-2">Errors</h3>
          {foreach $errors as $error}
            <li class="text-sm border-b border-red-100 pb-1 mb-1">{$error}</li>
          {/foreach}
        </ul>
      {/if}

      <label class="block mb-6">
        <span class="text-xs uppercase text-slate-500 font-semibold block mb-2"
          >E-mail</span
        >
        <input
          type="email"
          name="email"
          value="{$email}"
          class="block bg-slate-50 border border-slate-300 rounded-md shadow-inner px-3 py-1.5 w-full"
        />
      </label>

      <label class="block mb-6">
        <span class="text-xs uppercase text-slate-500 font-semibold block mb-2"
          >Name</span
        >
        <input
          type="text"
          name="name"
          value="{$name}"
          placeholder="John Smith"
          class="block bg-slate-50 border border-slate-300 rounded-md shadow-inner px-3 py-1.5 w-full"
        />
      </label>

      <label class="block mb-6">
        <span class="text-xs uppercase text-slate-500 font-semibold block mb-2"
          >Password</span
        >
        <input
          type="password"
          name="password"
          class="block bg-slate-50 border border-slate-300 rounded-md shadow-inner px-3 py-1.5 w-full"
        />
      </label>

      <label class="block mb-6">
        <span class="text-xs uppercase text-slate-500 font-semibold block mb-2"
          >Password again</span
        >
        <input
          type="password"
          name="password_again"
          class="block bg-slate-50 border border-slate-300 rounded-md shadow-inner px-3 py-1.5 w-full"
        />
      </label>

      <button
        type="submit"
        class="inline-block px-3 py-1.5 font-semibold shadow rounded-md bg-sky-600 text-white"
      >
        Create account
      </button>
    </form>
  </body>
</html>
